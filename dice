--- Dice

-- generates a random number between 1 and 6
main.random(6)

-- once you run the script, it "rolls"
-- a roll returns a random number between 1 and 6

--------------------

Dice = {}

function roll() {
  local roll_result = math.random(6)
  ***
  ***
  return roll_result
}

roll = function(n)
math.randomseed(os.time())
math.random(); math.random(); math.random()
print(math.random(n))
end
-- roll(6)

